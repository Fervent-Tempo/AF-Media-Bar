>  The English changelog is provided in  [CHANGELOG.en-US.md](CHANGELOG.en-US.md)。

# 更新日志

此文件记录 AF Media Bar 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，项目使用 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

## [Unreleased]

以可靠性修复为主：媒体会话自愈、频谱电平标定与歌词推进。

### 新增

- 歌词间距：歌词页可调双行行距（0–8 DIP）与字间距（0–20% 字号）。行距会按任务栏文字区的可用高度自动夹取，任何取值都不会把歌词挤出媒体栏；字距只改变观感，英文单词保持完整。

### 修复

- 媒体会话在换歌等场景下丢一次事件后永久失联：新增自动重连看门狗，会话关闭后的 30 秒内按 1 秒节奏自愈，平时按 5 秒兜底；第三方库连 ForceUpdate 也救不回来时自动重建媒体目录。
- 频谱在低音量下停在最小柱高不动：电平映射改为以参考峰值为基准的相对 dB 窗口，柱高不再随系统音量整体变矮；采集目标改为"实际出声的那个设备"（应用出声优先于系统混音回声，同档内保持当前端点），虚拟声卡把音乐路由到非默认端点时频谱不再静音（每 2 秒核对目标端点，变化后重建采集）。
- 播放器不上报播放进度（时间轴停在曲目起点）时歌词不换行：歌词选行改用与进度条一致的外推位置，并由既有的 250 ms 进度计时器推进。

### 改进

- 频谱分辨率与观感：FFT 点数改为随采样率自适应（96 kHz 由 512 点提升为 4096 点），频段值改为在功率谱上按分数 bin 线性插值积分（低频段不再与邻段共用同一个 bin，最左几列不再永远等高），并加入功率域 +3 dB/倍频程的倾斜补偿，高低频观感更均衡。

## [1.2.1] - 2026-09-21

以修复为主：任务栏自动隐藏动画、截图错位与多显示器显示。

### 新增

- 可在所有已启用的任务栏上同时显示。
- 逐字高亮开关：关闭后歌词仍按时间轴滚动，轨迹不变。

### 修复

- 任务栏自动隐藏动画：触边收起时的卡顿与跳位。
- 截图或录屏时媒体栏错位、折叠。
- 切换显示器后性能监测组件失效。
- 后台内存整理与日志队列释放的竞争。
- 设置页在说明条资源缺失时崩溃；若干窗口稳定性问题。
- 滚轮手势与提示收窄到封面与文字区；完整层不再省略信息。

## [1.2.0] - 2026-09-19

界面与交互重做后的第一个版本：任务栏歌词、安装程序与目标显示器选择是新增能力，设置页结构、外观与交互按功能重新组织。

### 新增

- 任务栏歌词：按网易云音乐、LRCLIB、QQ 音乐、酷狗音乐、汽水音乐依次匹配来源，支持逐字擦亮、未唱部分明暗、译文与音译、双行对齐；来源可在歌词页启停与排序。
- 安装程序：安装版可在程序内检查更新、后台下载并静默安装后续版本；便携版保持单文件、不写注册表。
- 目标显示器选择：可指定媒体栏跟随哪一块屏幕的任务栏，DPI 变化与 Explorer 重启后自动恢复。
- 快速启动：没有可接收的媒体时，点击或滚动音符打开启动列表，滚轮循环预览并在停止约 1.2 秒后启动最后一项。
- 当前播放曲目通知：曲目切换并开始播放时显示，位置、时长与显示器可调，可抑制全屏。
- SMTC 应用允许列表：可只接受选定应用发布的媒体会话。
- 音频控制面板：托盘图标左键与滚轮可绑定面板、设置或设备与音量。
- 新增频谱样式：波形图与像素柱状图。
- 关于页：开发人员名单（GitHub 贡献者，含头像）、赞助者名单、收款码与爱发电入口、开源许可清单。
- 后台内存占用自动压缩：空闲、关屏与睡眠时按档回收，启动安定后再回收一次，设置页可手动压缩内存。
- 程序日志与未处理异常捕获：可在设置页直接打开日志与设置目录。

### 改进

- 设置页重排为「显示模式、媒体与通知、交互、歌词、外观、应用」六项与页脚的「关于」，页面分组容器、标签条与固定页头同步重做，并支持按关键词搜索跳转。
- 外观统一：字体、字号、浅色/深色、窗口材质与材质浓度全局生效；主题色默认跟随系统强调色，也可自选或填十六进制值。
- 界面语言：简体中文、繁体中文与英文，默认跟随 Windows 显示语言，切换后立即生效，无需重启。
- 任务栏适配：封面保持自身宽高比，放不下的标题、歌手与歌词在各自区域内轮转滚动（正在擦亮的歌词行跟随亮区），无播放时可自动隐藏。
- 音频：输出设备列表稳定排序，当前媒体应用音量按 2% 调节，可查看空间音效并跳转系统声音设置。
- Windows 10：窗口材质与暗色标题栏按系统版本回退。
- 低性能降级：检测到软件渲染或低性能路径时自动关闭装饰性动效、滚动文字、频谱缓动与模糊。
- 更新检查只请求公开清单端点，不经过第三方代理；下载边下载边校验 SHA-256，直连失败后按清单里的加速地址重试。

### 修复

- 修复手动选择播放来源后仍会被自动跟随覆盖的问题。
- 修复悬停进入时跑马灯被顶掉、悬停层收起后无法打开完整层的问题。
- 修复媒体区域滚轮提示不出现、快速启动预览缺少提示的问题。
- 修复 Windows 10 任务栏搜索框区域未正确避让，以及手动拖动媒体栏时的位置偏移。
- 修复媒体会话在播放中被关闭时可能出现的崩溃，以及第一条曲目通知的尺寸异常。

### 兼容性与限制

- 竖向任务栏与悬浮模式不再提供：本版本只支持横向任务栏，设置中的灵动岛、桌面卡片与悬浮球为占位选项，选中只切换页内显示。
- 网格布局编辑器不再出现在设置里：1.1.1 及更早版本的布局档案（`%LOCALAPPDATA%\AFMediaBar\profiles\layout.json`，schema 5）不再使用，界面回到固定布局。
- **升级后设置会回到默认值**：设置文件只读取本版本的编号（2），1.1.1 写出的旧编号文件不再读取，而是被改名留档为 `settings.json.unsupported-<时间戳>`；「我的默认设置」快照同样处理。文件一律不删除，可以手工找回，但设置需要重新配置一次。
- 1.1.1 → 1.2.0 需要手动下载：本版本的公开清单未提供可自动安装的安装包，程序内自动下载与静默安装从 1.2.1 起恢复。
- 当前仅发布 `win-x64` 版本，尚未提供 ARM64 构建；发布文件尚未进行商业代码签名，Windows SmartScreen 可能显示“未知发布者”。

## [1.1.1] - 2026-08-17

### 改进

- 完善简体中文、繁体中文和英文界面的即时切换。
- 优化设置窗口、诊断日志和字体预设切换体验。
- 改进长度、间距、厚度、独立尺寸、字体粗细和任务栏垂直偏移调节。
- 优化媒体内容显示、封面圆角调节和布局自动切换。
- 增加资源监控区域的任务管理器快捷操作。
- 移除旧版注册表兼容逻辑，简化设置读取路径。

### 修复

- 修复悬浮窗口意外消失和焦点干扰问题。
- 修复浏览器媒体封面刷新和断线宽限期内的媒体切换问题。
- 修复托盘媒体唤醒、桌面边缘尺寸重锚和相关窗口恢复行为。
- 改进媒体暂停后的播放来源自动切换。

## [1.1.0] - 2026-08-14

### 新增

- 增加 Windows 10 兼容支持。
- 增加系统主题自动跟随和独立主题设置。
- 增加横向与竖向任务栏播放器布局。
- 增加浮动窗口、边缘收起和窗口可见性选项。
- 增加自动更新检查、备用版本清单源和跳过版本功能。

### 变更

- 重构设置窗口和设置菜单。
- 重构任务栏窗口托管，提高不同任务栏布局下的适配能力。
- 改进双语社区和项目文档。

### 修复

- 修复右键菜单层级问题。

## [1.0.1] - 2026-08-10

### 修复

- 通过 Shell 事件跟踪、原始任务栏几何观察和合成帧更新，降低自动隐藏任务栏展开与收回的延迟。
- 保留全屏隐藏行为，同时避免在正常任务栏自动隐藏过渡期间不必要地销毁窗口。

### 变更

- 替换应用程序和 README 品牌图标。
- 将自包含 `win-x64` Release 包改为单个可执行文件，不再包含数百个运行时文件。
- 记录当前自动隐藏动画限制，并推荐使用固定任务栏配置。

## [1.0.0] - 2026-08-09

### 新增

- GSMTC 媒体发现、来源切换、元数据、封面图和播放控制。
- Windows 11 任务栏定位、自动隐藏跟踪、全屏隐藏和托盘集成。
- 默认输出设备切换，以及所选媒体应用音量控制。
- WASAPI 回环音频可视化，以及可选的系统/进程指标。
- 低配置渲染模式和开机启动支持。
- 中文和英文文档，以及自包含 `win-x64` 发布自动化。

### 变更

- 将产品重命名为 AF Media Bar，并将可执行文件命名为 `AFMediaBar.exe`。
- 限制封面图缓冲和长期运行的媒体音量来源缓存。

### 安全

- 将原生库查找限制到 System32。
- 移除对媒体提供的 `.exe` 来源标识符的通用执行。

[1.2.1]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.2.0...v1.2.1
[1.2.0]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.1.1...v1.2.0
[1.1.1]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.0.1...v1.1.0
[1.0.1]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/Fervent-Tempo/AF-Media-Bar/releases/tag/v1.0.0
