>  The English changelog is provided in  [CHANGELOG.en-US.md](CHANGELOG.en-US.md)。

# 更新日志

此文件记录 AF Media Bar 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，项目使用 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

## [Unreleased]

### 计划

- 视频字幕/歌词滚动展示。
- 媒体进度条展示。
- UI 美化、提供多种预制主题。
- 配置导出与分享。
- 添加新手教程引导。

### 新增

- 布局档案改为任务栏与悬浮共享的横向、竖向两套；宿主模式与排布方向分开选择，方向由当前档案统一决定。
- 设置窗口改为可拖放拼贴编辑器，长条内可自由组合常驻/悬停切换容器，边缘折叠容器只编辑展开内容；主要属性优先显示，并支持按档案隔离的撤销和单档案重置。
- 布局 JSON 升级到 schema 3；schema 1/2 按迁移时当前宿主模式保留对应横竖档案，并继续使用校验钳制、原子写入、备份和损坏文件恢复。
- 增加封面、媒体文本、控制、指标、频谱和分隔线组件的尺寸与专属数值设置，并补齐三语言资源和搜索索引。
- 组件属性面板优先显示主要选项，支持只重置当前组件默认值；媒体文本支持最大行数、宽度限制和省略，调色板直接显示歌曲名、歌手及具体播放控制名称。
- 长条空白区域固定可拖动，任务栏拖动会临时退出自动定位/位置锁定；输出设备和媒体音量组件支持鼠标滚轮；设置窗口控件改为响应式宽度。
- 容器增加内容对齐选项（居中、靠前、靠后、填满），边缘折叠状态移除展开内容子树以关闭隐藏组件的命中区域。
- 修复多行歌曲名仍被跑马灯改回单行，以及重置“上一首/歌手”组件时误变成“播放/歌曲名”的问题；旧容器方向统一迁移为跟随档案。
- 悬停容器改为严格按指针切换离开/靠近槽位，新增歌曲名与歌手两行信息组件；最多显示行数移入高级显示且不再改变容器高度。
- 设置布局编辑器改为插入区、画布、属性区三栏工作区；容器支持恢复默认行为，边缘折叠收起后只保留触发区并继续保持长条空白可拖动。
- 预览画布改为完整组合比例显示，设置窗口为编辑场景提供更宽的自适应画布，窄窗口也不会被属性栏顶出或裁剪。
- 设计拖放增加释放区域高亮和真实组件选中框；空边缘不再显示只属于编辑器的方向标签，保持预览与运行时视觉一致。
- 悬停容器在编辑器中会按当前选择的离开/靠近槽即时切换预览，容器空白区域可直接选中容器本身。
- 拖放目标改为真实容器槽位命中：鼠标悬停哪个容器就落入哪个槽位，运行时组件树不绑定编辑器拖放事件。
- 设置预览改用运行时真实组件树并按完整组合画布自动缩放；组件库和拖动反馈显示真实图形，选中对象显示不影响测量的设计边框。
- 修正折叠边缘容器的拖动碰撞：触发条仅保留少量可见像素，长条本体可贴到屏幕边缘，展开内容才重新计入窗口边界。
- 修正多个悬停切换容器共用鼠标状态导致的闪烁；每个容器按自身命中状态独立切换离开/靠近槽。
- 悬停容器的靠近触发距离现在真正参与鼠标命中计算，并在高级行为中提供 DIP 数值调节；边缘容器重建时恢复淡入动画。
- 组件表面补充透明命中背景并统一距离判定，悬停容器在空白区域靠近时也能稳定切换；默认媒体容器的靠近距离为 48 DIP。
- 宿主窗口的 Win32 输入区域改为条带、展开内容和触发条的并集，折叠内容不再占用透明碰撞区域；组件和容器恢复默认时同时清除手动尺寸/偏移覆盖。
- 文本最多显示行数限制为 1–2 行；两行歌曲名/歌手组件不再显示无效的跑马灯和行数选项，设置面板三栏宽度按窗口自适应。
- 悬停离开切换改为单向淡入靠近内容、离开内容立即落地，避免动画结束后旧组件闪回；编辑器新增当前布局对象列表和持续容器边界，空折叠容器也可直接选中。
- 自动任务栏排布方向改为复用主窗口的任务栏几何判断，横向任务栏的编辑预览不再误显示为竖向；编辑器画布区域固定为响应式工作区，缩小窗口时由各栏内部滚动。

### 兼容性

- 悬停离开态禁止交互组件；边缘折叠时完全隐藏展开内容，只保留触发区域，避免不可见状态触发媒体与设备操作。
- 布局尺寸由长条与边缘组合估算，保留旧窗口节点作为弹窗锚点回退；Windows 10 1809+ 最低版本不变。
- 旧组件注册表选项仅在首次启动时用于迁移，随后清理；组件配置的唯一来源变为 `%LOCALAPPDATA%\AFMediaBar\profiles\layout.json`。

## [1.1.1] - 2026-08-17

### 改进

- 完善简体中文、繁体中文和英文界面的即时切换。
- 优化设置窗口、诊断日志和字体预设切换体验。
- 改进长度、间距、厚度、独立尺寸、字体粗细和任务栏垂直偏移调节。
- 优化媒体内容显示、封面圆角调节和布局自动切换。
- 增加资源监控区域的任务管理器快捷操作。
- 移除旧版注册表兼容逻辑，简化设置读取路径。

### 修复

- 修复悬浮窗口意外消失和焦点干扰问题。
- 修复浏览器媒体封面刷新和断线宽限期内的媒体切换问题。
- 修复托盘媒体唤醒、桌面边缘尺寸重锚和相关窗口恢复行为。
- 改进媒体暂停后的播放来源自动切换。

## [1.1.0] - 2026-08-14

### 新增

- 增加 Windows 10 兼容支持。
- 增加系统主题自动跟随和独立主题设置。
- 增加横向与竖向任务栏播放器布局。
- 增加浮动窗口、边缘收起和窗口可见性选项。
- 增加自动更新检查、备用版本清单源和跳过版本功能。

### 变更

- 重构设置窗口和设置菜单。
- 重构任务栏窗口托管，提高不同任务栏布局下的适配能力。
- 改进双语社区和项目文档。

### 修复

- 修复右键菜单层级问题。

## [1.0.1] - 2026-08-10

### 修复

- 通过 Shell 事件跟踪、原始任务栏几何观察和合成帧更新，降低自动隐藏任务栏展开与收回的延迟。
- 保留全屏隐藏行为，同时避免在正常任务栏自动隐藏过渡期间不必要地销毁窗口。

### 变更

- 替换应用程序和 README 品牌图标。
- 将自包含 `win-x64` Release 包改为单个可执行文件，不再包含数百个运行时文件。
- 记录当前自动隐藏动画限制，并推荐使用固定任务栏配置。

## [1.0.0] - 2026-08-09

### 新增

- GSMTC 媒体发现、来源切换、元数据、封面图和播放控制。
- Windows 11 任务栏定位、自动隐藏跟踪、全屏隐藏和托盘集成。
- 默认输出设备切换，以及所选媒体应用音量控制。
- WASAPI 回环音频可视化，以及可选的系统/进程指标。
- 低配置渲染模式和开机启动支持。
- 中文和英文文档，以及自包含 `win-x64` 发布自动化。

### 变更

- 将产品重命名为 AF Media Bar，并将可执行文件命名为 `AFMediaBar.exe`。
- 限制封面图缓冲和长期运行的媒体音量来源缓存。

### 安全

- 将原生库查找限制到 System32。
- 移除对媒体提供的 `.exe` 来源标识符的通用执行。

[Unreleased]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.1.1...HEAD
[1.1.1]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.0.1...v1.1.0
[1.0.1]: https://github.com/Fervent-Tempo/AF-Media-Bar/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/Fervent-Tempo/AF-Media-Bar/releases/tag/v1.0.0
